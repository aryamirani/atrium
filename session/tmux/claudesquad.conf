# claude-squad managed tmux config. Regenerated on launch — do NOT edit.
# To customize, set "tmux_config_override" in ~/.claude-squad/config.json.
#
# cs runs its sessions on a dedicated tmux socket (-L claudesquad) started with
# this file (-f), so the host ~/.tmux.conf is never sourced. Everything cs relies
# on is therefore set explicitly below.

# Stable, human-readable window names (never auto-rename from the running program).
set-option -g  allow-rename       off
set-option -g  automatic-rename   off
set-window-option -g automatic-rename off
set-option -g  set-titles         off

# Scrollback + mouse (cs used to set these per-session after creation; now global).
set-option -g  history-limit      10000
set-option -g  mouse              on

# No status bar — each cs session is a single fullscreen pane and the agent draws
# its own UI, so tmux chrome is pure noise.
set-option -g  status             off

# Color fidelity: cs captures pane content with -e (ANSI preserved) and re-renders
# it, so the isolated server must advertise 256-color + truecolor passthrough.
set-option -g  default-terminal   "tmux-256color"
set-option -ga terminal-overrides ",*:Tc"

# Predictable behavior regardless of host preferences.
set-option -g  base-index         0
set-window-option -g pane-base-index 0
set-option -sg escape-time        10
set-option -g  destroy-unattached off
set-option -g  remain-on-exit     off
